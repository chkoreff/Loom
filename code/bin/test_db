#!/usr/bin/perl -w
use strict;

=pod

=head1 NAME

Test program for in-memory DB operations

=cut

use FindBin;
my $TOP;
BEGIN { $TOP = "$FindBin::RealBin/../.." }

use lib "$TOP/code";

use Loom::Test::DB;

Loom::Test::DB->new($TOP)->run;
